import { prisma } from '@/lib/prisma';
export default async function Customers(){const users=await prisma.user.findMany({where:{role:'USER'},include:{_count:{select:{orders:true}},orders:{select:{total:true}}},orderBy:{createdAt:'desc'}});return <><div className="eyebrow" style={{color:'var(--coral)'}}>People</div><h1>Customers</h1>{users.length?<table className="table"><thead><tr><th>Name</th><th>Email</th><th>Phone</th><th>Orders</th><th>Total spent</th><th>Joined</th></tr></thead><tbody>{users.map(user=><tr key={user.id}><td>{user.name}</td><td>{user.email}</td><td>{user.phone||'—'}</td><td>{user._count.orders}</td><td>৳{user.orders.reduce((sum,order)=>sum+Number(order.total),0).toLocaleString()}</td><td>{user.createdAt.toLocaleDateString()}</td></tr>)}</tbody></table>:<p className="muted">No customers yet.</p>}</>}
