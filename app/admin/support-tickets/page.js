export default function SupportTickets() {
  return <><div className="eyebrow">Customer service</div><h1>Support tickets</h1><section className="section admin-empty-state"><h2>Support ticket storage is not configured</h2><p className="muted">No ticket table exists in the live database, so no fake tickets are shown. Existing customer contact flows remain available.</p></section></>;
}
