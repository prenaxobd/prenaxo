import AdminModuleState from '@/components/admin/AdminModuleState';
export default function Redirects() { return <AdminModuleState eyebrow="SEO" title="Redirects" heading="Redirect management needs setup" message="Redirects require the additive Redirect table, which is not present in the live database." />; }
