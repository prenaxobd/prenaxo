import AdminModuleState from '@/components/admin/AdminModuleState';
export default function Schema() { return <AdminModuleState eyebrow="SEO" title="Structured data" heading="Structured data is ready for setup" message="Schema configuration will be available after SEO metadata is connected." />; }
