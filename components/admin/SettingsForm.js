'use client';
import { useState } from 'react';
export default function SettingsForm({initialSettings}){const [form,setForm]=useState({siteName:initialSettings?.siteName||'Prenaxo',contactEmail:initialSettings?.contactEmail||'',contactPhone:initialSettings?.contactPhone||'',currency:initialSettings?.currency||'BDT',shippingCharge:initialSettings?.shippingCharge||80});const [message,setMessage]=useState('');async function save(e){e.preventDefault();const r=await fetch('/api/admin/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const data=await r.json();setMessage(r.ok?'Settings saved.':data.error||'Unable to save settings.')}return <form className="form" onSubmit={save} style={{marginTop:25}}><label>Store name<input required value={form.siteName} onChange={e=>setForm({...form,siteName:e.target.value})}/></label><label>Store email<input type="email" value={form.contactEmail} onChange={e=>setForm({...form,contactEmail:e.target.value})}/></label><label>Phone<input value={form.contactPhone} onChange={e=>setForm({...form,contactPhone:e.target.value})}/></label><label>Currency<input required value={form.currency} onChange={e=>setForm({...form,currency:e.target.value})}/></label><label>Shipping charge<input type="number" min="0" value={form.shippingCharge} onChange={e=>setForm({...form,shippingCharge:e.target.value})}/></label><button className="btn">Save settings</button>{message&&<p role="status">{message}</p>}</form>}
